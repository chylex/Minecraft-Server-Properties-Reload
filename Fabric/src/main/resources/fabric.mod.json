{
  "schemaVersion": 1,
  "id": "serverpropertiesreload",
  "name": "${name}",
  "description": "${description}",
  "version": "${version}",
  "license": "${license}",
  
  "authors": [
    "${author}"
  ],
  
  "contact": {
    "issues": "${issuesURL}",
    "sources": "${sourcesURL}"
  },
  
  "environment": "server",
  "mixins": [
    "serverpropertiesreload.mixins.json"
  ],
  
  "entrypoints": {
    "servertest": [
      "chylex.serverproperties.test.TestFinder"
    ]
  }
}
